#pragma once
#include <chrono>
#include <iostream>

class Timer {
public:
    void update() {
        //Use delta time to have a timer displaying the time on the top right corner of the screen

        //SDL_GetTicks() returns the number of milliseconds since the SDL library was initialized

        //fps using SDL_GetTicks()

    }
};